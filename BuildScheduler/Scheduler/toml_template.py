# vire.toml: Ignore structure/syntax, research not done yet.
"""
`vire.toml`. temp structure to figure out what I'm dealing with.

[details]
framework = 'next | alpine | html | vite |..etc'
package_manager = 'npm | pnpm | ...etc'

[project]
name = "example build format"
package_json = "./builds/package.json"
lockfile = "./builds/lockfile.(json|lock|yaml)"
config = "./builds/next.config.js | ..etc"
"""
